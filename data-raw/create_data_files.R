library(dplyr)
library(devtools)
library(RPostgreSQL)
# # Create a table of usernames and passwords
# users <- data_frame(username = letters,
#                     password = 1:26)
# use_data(users,
#          overwrite = TRUE)
# 

